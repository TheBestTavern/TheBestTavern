# 사용자 지정 디렉토리 설정
excelDirectoryPath=excel_files # 엑셀 파일 디렉토리 경로
loaderOutputDirectory=loader_output # 로더 클래스 출력 디렉토리 경로
jsonOutputDirectory=json_output # JSON 파일 출력 디렉토리 경로

# 경로 타입 선택
useAbsolutePath = false # 절대 경로를 사용할지 여부 (true: 절대 경로, false: 실행 파일 기준 상대 경로)

# 다중 시트 설정
allowMultipleSheets=false # 다중 시트를 허용할지 여부 (true/false)

# Resources 사용 설정
useResources=true # Resources 폴더 사용 여부 (true/false)
resourcesInternalPath=JSON # Resources 내부 경로
